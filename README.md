# csv-validator

## Uso

Ejemplo con flags:

```bash
node src/validate.js --input data/sample.csv --output out/report.json
```

Ejemplo compatible con el argumento posicional:

```bash
node src/validate.js data/sample.csv
```
